import React from 'react';
import {paneStyle} from '../modules/styles'


const LoadButton = () => {

    return (
        <div style={{height: "20px", width: "40px", backgroundColor: "gray"}}>
            <p>Load Board</p>
        </div>
    );
};

export default LoadButton;